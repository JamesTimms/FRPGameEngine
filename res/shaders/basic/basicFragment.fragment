#version 330

in vec2 texCoord0;

uniform vec3 color;
uniform float balance;
uniform sampler2D sampler;

out vec4 fragColour;

void main() {
    vec4 sample = texture2D( sampler, texCoord0 );
    fragColour = sample * vec4( color, 1 ) * balance;
}
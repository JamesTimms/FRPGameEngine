#version 330

in vec2 texCoord0;

uniform vec3 color;
uniform sampler2D sampler;

out vec4 fragColour;

void main() {
    vec2 mult = texCoord0;//(2.0*texCoord0 - 1.0)/(2.0*1.0);
    fragColour = texture2D( sampler, mult );// * vec4( color, 1 );
}
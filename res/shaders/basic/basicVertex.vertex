#version 330

layout (location = 0) in vec3 position;
layout (location = 1) in vec2 texCoord;

out vec2 texCoord0;
uniform mat4 transform;

void main() {
    texCoord0 = position.xy + vec2(0.5, 0.5);//(position.xy * 0.5) + (vec2(0.5, 0.5));
    gl_Position = transform * vec4( position, 1.0 );
}